{
  "name": "agentnet",
  "version": "0.1.0",
  "private": true,
  "description": "A decentralized AI agent swarm on 0G Chain",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "test": "turbo test",
    "typecheck": "turbo typecheck",
    "lint": "turbo lint",
    "seed": "ts-node scripts/seed.ts",
    "start": "ts-node scripts/orchestrator.ts",
    "demo": "ts-node scripts/demo.ts",
    "clean": "turbo clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "ts-node": "^10.9.0",
    "@types/node": "^20.0.0"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20.0.0"
  }
}
