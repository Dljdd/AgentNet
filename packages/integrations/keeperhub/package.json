{
  "name": "@agentnet/integrations-keeperhub",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "dependencies": {
    "@agentnet/types": "workspace:*",
    "@agentnet/core": "workspace:*",
    "@agentnet/integrations-uniswap": "workspace:*"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  }
}
