{
  "name": "@agentnet/mcp-server",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "dependencies": {
    "@agentnet/types": "workspace:*",
    "@agentnet/core": "workspace:*",
    "@agentnet/agents-worker": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "start": "ts-node src/index.ts"
  }
}
